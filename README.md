Run the command "npm install" both in your API project directory and UI project directory before running the project.

API project is written by Express JS using BCE structure.
UI project is written by React JS and Tailwind as CSS framework.

To run API project: first go into the API project directory and use the command "node app.js"
To run UI project: first go into the UI project directory and use the command "npm run dev"
